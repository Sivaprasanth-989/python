alpha_dict = {}
for i in range(26):
    alpha_dict[chr(97+i)] = i+1
    
s = input().split()
result = []
for word in s:
    temp = []
    for j in word.lower():
        if j in alpha_dict:
             temp.append(str(alpha_dict[j]))
    result.append("-".join(temp))
print(" ".join(result))
